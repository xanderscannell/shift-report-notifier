<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- "Display over other apps". This grants a background-activity-launch
         exemption so AlarmReceiver can start FormActivity directly even while
         the phone is unlocked and in use. Without it, a full-screen intent is
         demoted to a heads-up notification whenever the screen is unlocked. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Lets the app ask to be exempted from battery optimization so aggressive
         OEM battery managers don't kill the daily alarm. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Exact-alarm permissions. This is a reminder/alarm app, so USE_EXACT_ALARM
         (API 33+) is appropriate and is auto-granted with no user prompt.
         SCHEDULE_EXACT_ALARM covers API 31-32 (also auto-granted there).
         Without these, setAlarmClock() throws SecurityException on Android 13+. -->
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.DayNight">

        <!-- The form screen is the launcher: tapping the app icon opens the
             form directly. excludeFromRecents + the lock-screen flags (also
             set in code) make it behave like an alarm/call screen. -->
        <activity
            android:name=".FormActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:showWhenLocked="true"
            android:turnScreenOn="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".AlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
